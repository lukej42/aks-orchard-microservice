apiVersion: v1
kind: Service
metadata:
  name: {{ include "orchard-chart.fullname" . }}-svc
  labels:
    {{- include "orchard-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orchard-chart.selectorLabels" . | nindent 4 }}
